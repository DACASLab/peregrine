# ╔══════════════════════════════════════════════════════════════════════════════╗
# ║  docker-compose.dev.yml — Dev override (mounts host source into container) ║
# ║                                                                            ║
# ║  Layer on top of any target compose file:                                  ║
# ║    docker compose \                                                        ║
# ║      -f compose/docker-compose.simulation.yml \                            ║
# ║      -f compose/docker-compose.dev.yml \                                   ║
# ║      up                                                                    ║
# ╚══════════════════════════════════════════════════════════════════════════════╝

services:
  sim:
    volumes:
      - ../../src:/ros2_ws/src:rw
