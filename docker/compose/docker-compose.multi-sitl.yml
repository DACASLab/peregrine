# AUTO-GENERATED by docker/scripts/generate_multi_sitl.py. DO NOT EDIT.
# Regenerate with: make generate-multi

x-common: &common
  image: ${PROJECT_NAME:-ros2-px4-flight}-simulation:latest
  network_mode: host
  ipc: host
  privileged: true
  stdin_open: true
  tty: true
  volumes:
    - ../../:${ROS_WS:-/ros2_ws}:rw
    - /tmp/.X11-unix:/tmp/.X11-unix:rw
    - ${HOME}/.Xauthority:/home/${CONTAINER_USER:-peregrine}/.Xauthority:ro

services:
  # -- Shared Gazebo + PX4 SITL instances --------------------------------------
  sim:
    <<: *common
    container_name: ${PROJECT_NAME:-ros2-px4-flight}-multi-sim
    environment:
      - DISPLAY=${DISPLAY:-:0}
      - RMW_IMPLEMENTATION=${RMW_IMPLEMENTATION:-rmw_cyclonedds_cpp}
      - ROS_LOCALHOST_ONLY=1
      - ROS_WS=${ROS_WS:-/ros2_ws}
      - PX4_GZ_WORLD=${PX4_GZ_WORLD:-default}
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu, compute, utility, graphics]
    command: >
      bash -lc "
        source /opt/ros/${ROS_DISTRO:-humble}/setup.bash &&
        [ -f ${ROS_WS:-/ros2_ws}/install/setup.bash ] && source ${ROS_WS:-/ros2_ws}/install/setup.bash;
        ros2 launch peregrine_bringup multi_uav_sitl.launch.py num_uavs:=3 px4_gz_world:=${PX4_GZ_WORLD:-default} uav_spawn_spacing_m:=${UAV_SPAWN_SPACING_M:-5.0} px4_base_delay:=${PX4_BASE_DELAY:-8.0} px4_stagger_delay:=${PX4_STAGGER_DELAY:-5.0} headless:=${HEADLESS:-1}
      "

  # -- UAV 1: Peregrine stack + Zenoh bridge --------------------------------
  uav1:
    <<: *common
    container_name: ${PROJECT_NAME:-ros2-px4-flight}-uav1
    environment:
      - DISPLAY=${DISPLAY:-:0}
      - DRONE_ID=1
      - RMW_IMPLEMENTATION=${RMW_IMPLEMENTATION:-rmw_cyclonedds_cpp}
      - ROS_LOCALHOST_ONLY=1
      - ROS_WS=${ROS_WS:-/ros2_ws}
      - ZENOH_BRIDGE_CONFIG=${ROS_WS:-/ros2_ws}/docker/config/zenoh/uav_bridge.json5
      - ZENOH_PORT=7447
    depends_on:
      sim:
        condition: service_started
    command: >
      bash -lc "
        echo '[uav1] Waiting for MicroXRCEAgent UDP port 8888...' &&
        timeout 90 bash -c 'until ss -lnup | grep -q \":8888 \"; do sleep 1; done' &&
        sleep 2 &&
        source /opt/ros/${ROS_DISTRO:-humble}/setup.bash &&
        [ -f ${ROS_WS:-/ros2_ws}/install/setup.bash ] && source ${ROS_WS:-/ros2_ws}/install/setup.bash;
        ros2 launch peregrine_bringup core_stack.launch.py start_microxrce_agent:=false ros_domain_id:=1 microxrce_port:=8888 uav_namespace:=/uav1 target_system_id:=1 use_sim_time:=true
      "
  # -- UAV 2: Peregrine stack + Zenoh bridge --------------------------------
  uav2:
    <<: *common
    container_name: ${PROJECT_NAME:-ros2-px4-flight}-uav2
    environment:
      - DISPLAY=${DISPLAY:-:0}
      - DRONE_ID=2
      - RMW_IMPLEMENTATION=${RMW_IMPLEMENTATION:-rmw_cyclonedds_cpp}
      - ROS_LOCALHOST_ONLY=1
      - ROS_WS=${ROS_WS:-/ros2_ws}
      - ZENOH_BRIDGE_CONFIG=${ROS_WS:-/ros2_ws}/docker/config/zenoh/uav_bridge.json5
      - ZENOH_PORT=7448
    depends_on:
      sim:
        condition: service_started
    command: >
      bash -lc "
        echo '[uav2] Waiting for MicroXRCEAgent UDP port 8890...' &&
        timeout 90 bash -c 'until ss -lnup | grep -q \":8890 \"; do sleep 1; done' &&
        sleep 2 &&
        source /opt/ros/${ROS_DISTRO:-humble}/setup.bash &&
        [ -f ${ROS_WS:-/ros2_ws}/install/setup.bash ] && source ${ROS_WS:-/ros2_ws}/install/setup.bash;
        ros2 launch peregrine_bringup core_stack.launch.py start_microxrce_agent:=false ros_domain_id:=2 microxrce_port:=8890 uav_namespace:=/uav2 px4_namespace:=/px4_1 target_system_id:=2 use_sim_time:=true
      "
  # -- UAV 3: Peregrine stack + Zenoh bridge --------------------------------
  uav3:
    <<: *common
    container_name: ${PROJECT_NAME:-ros2-px4-flight}-uav3
    environment:
      - DISPLAY=${DISPLAY:-:0}
      - DRONE_ID=3
      - RMW_IMPLEMENTATION=${RMW_IMPLEMENTATION:-rmw_cyclonedds_cpp}
      - ROS_LOCALHOST_ONLY=1
      - ROS_WS=${ROS_WS:-/ros2_ws}
      - ZENOH_BRIDGE_CONFIG=${ROS_WS:-/ros2_ws}/docker/config/zenoh/uav_bridge.json5
      - ZENOH_PORT=7449
    depends_on:
      sim:
        condition: service_started
    command: >
      bash -lc "
        echo '[uav3] Waiting for MicroXRCEAgent UDP port 8892...' &&
        timeout 90 bash -c 'until ss -lnup | grep -q \":8892 \"; do sleep 1; done' &&
        sleep 2 &&
        source /opt/ros/${ROS_DISTRO:-humble}/setup.bash &&
        [ -f ${ROS_WS:-/ros2_ws}/install/setup.bash ] && source ${ROS_WS:-/ros2_ws}/install/setup.bash;
        ros2 launch peregrine_bringup core_stack.launch.py start_microxrce_agent:=false ros_domain_id:=3 microxrce_port:=8892 uav_namespace:=/uav3 px4_namespace:=/px4_2 target_system_id:=3 use_sim_time:=true
      "
