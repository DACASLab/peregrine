# AUTO-GENERATED by docker/scripts/generate_multi_sitl.py. DO NOT EDIT.
# Regenerate with: make generate-multi

x-common: &common
  image: ${PROJECT_NAME:-ros2-px4-flight}-simulation:latest
  network_mode: host
  ipc: host
  privileged: true
  stdin_open: true
  tty: true
  volumes:
    - ../../:${ROS_WS:-/ros2_ws}:rw
    - /tmp/.X11-unix:/tmp/.X11-unix:rw
    - ${HOME}/.Xauthority:/home/${CONTAINER_USER:-peregrine}/.Xauthority:ro

services:
  # -- Shared Gazebo + PX4 SITL instances --------------------------------------
  sim:
    <<: *common
    container_name: ${PROJECT_NAME:-ros2-px4-flight}-multi-sim
    environment:
      - DISPLAY=${DISPLAY:-:0}
      - ROS_LOCALHOST_ONLY=1
      - ROS_WS=${ROS_WS:-/ros2_ws}
      - PX4_GZ_WORLD=${PX4_GZ_WORLD:-default}
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu, compute, utility, graphics]
    command: >
      bash -lc "
        source /opt/ros/${ROS_DISTRO:-humble}/setup.bash &&
        [ -f ${ROS_WS:-/ros2_ws}/install/setup.bash ] && source ${ROS_WS:-/ros2_ws}/install/setup.bash;
        ros2 launch peregrine_bringup multi_uav_sitl.launch.py num_uavs:=9 px4_gz_world:=${PX4_GZ_WORLD:-default} uav_spawn_spacing_m:=${UAV_SPAWN_SPACING_M:-5.0} px4_base_delay:=${PX4_BASE_DELAY:-8.0} px4_stagger_delay:=${PX4_STAGGER_DELAY:-5.0} headless:=0
      "

  # -- UAV 1: Peregrine stack + Zenoh bridge --------------------------------
  uav1:
    <<: *common
    container_name: ${PROJECT_NAME:-ros2-px4-flight}-uav1
    environment:
      - DISPLAY=${DISPLAY:-:0}
      - DRONE_ID=1
      - ROS_LOCALHOST_ONLY=1
      - ROS_WS=${ROS_WS:-/ros2_ws}
      - ZENOH_BRIDGE_CONFIG=${ROS_WS:-/ros2_ws}/docker/config/zenoh/uav_bridge.json5
      - ZENOH_PORT=7447
    depends_on:
      sim:
        condition: service_started
    command: >
      bash -lc "
        echo '[uav1] Waiting for MicroXRCEAgent UDP port 8888...' &&
        timeout 90 bash -c 'until ss -lnup | grep -q \":8888 \"; do sleep 1; done' &&
        sleep 2 &&
        source /opt/ros/${ROS_DISTRO:-humble}/setup.bash &&
        [ -f ${ROS_WS:-/ros2_ws}/install/setup.bash ] && source ${ROS_WS:-/ros2_ws}/install/setup.bash;
        ros2 launch peregrine_bringup single_uav.launch.py start_microxrce_agent:=false ros_domain_id:=1 microxrce_port:=8888 uav_namespace:=/uav1 target_system_id:=1 use_sim_time:=true
      "
  # -- UAV 2: Peregrine stack + Zenoh bridge --------------------------------
  uav2:
    <<: *common
    container_name: ${PROJECT_NAME:-ros2-px4-flight}-uav2
    environment:
      - DISPLAY=${DISPLAY:-:0}
      - DRONE_ID=2
      - ROS_LOCALHOST_ONLY=1
      - ROS_WS=${ROS_WS:-/ros2_ws}
      - ZENOH_BRIDGE_CONFIG=${ROS_WS:-/ros2_ws}/docker/config/zenoh/uav_bridge.json5
      - ZENOH_PORT=7448
    depends_on:
      sim:
        condition: service_started
    command: >
      bash -lc "
        echo '[uav2] Waiting for MicroXRCEAgent UDP port 8890...' &&
        timeout 90 bash -c 'until ss -lnup | grep -q \":8890 \"; do sleep 1; done' &&
        sleep 2 &&
        source /opt/ros/${ROS_DISTRO:-humble}/setup.bash &&
        [ -f ${ROS_WS:-/ros2_ws}/install/setup.bash ] && source ${ROS_WS:-/ros2_ws}/install/setup.bash;
        ros2 launch peregrine_bringup single_uav.launch.py start_microxrce_agent:=false ros_domain_id:=2 microxrce_port:=8890 uav_namespace:=/uav2 px4_namespace:=/px4_1 target_system_id:=2 use_sim_time:=true
      "
  # -- UAV 3: Peregrine stack + Zenoh bridge --------------------------------
  uav3:
    <<: *common
    container_name: ${PROJECT_NAME:-ros2-px4-flight}-uav3
    environment:
      - DISPLAY=${DISPLAY:-:0}
      - DRONE_ID=3
      - ROS_LOCALHOST_ONLY=1
      - ROS_WS=${ROS_WS:-/ros2_ws}
      - ZENOH_BRIDGE_CONFIG=${ROS_WS:-/ros2_ws}/docker/config/zenoh/uav_bridge.json5
      - ZENOH_PORT=7449
    depends_on:
      sim:
        condition: service_started
    command: >
      bash -lc "
        echo '[uav3] Waiting for MicroXRCEAgent UDP port 8892...' &&
        timeout 90 bash -c 'until ss -lnup | grep -q \":8892 \"; do sleep 1; done' &&
        sleep 2 &&
        source /opt/ros/${ROS_DISTRO:-humble}/setup.bash &&
        [ -f ${ROS_WS:-/ros2_ws}/install/setup.bash ] && source ${ROS_WS:-/ros2_ws}/install/setup.bash;
        ros2 launch peregrine_bringup single_uav.launch.py start_microxrce_agent:=false ros_domain_id:=3 microxrce_port:=8892 uav_namespace:=/uav3 px4_namespace:=/px4_2 target_system_id:=3 use_sim_time:=true
      "
  # -- UAV 4: Peregrine stack + Zenoh bridge --------------------------------
  uav4:
    <<: *common
    container_name: ${PROJECT_NAME:-ros2-px4-flight}-uav4
    environment:
      - DISPLAY=${DISPLAY:-:0}
      - DRONE_ID=4
      - ROS_LOCALHOST_ONLY=1
      - ROS_WS=${ROS_WS:-/ros2_ws}
      - ZENOH_BRIDGE_CONFIG=${ROS_WS:-/ros2_ws}/docker/config/zenoh/uav_bridge.json5
      - ZENOH_PORT=7450
    depends_on:
      sim:
        condition: service_started
    command: >
      bash -lc "
        echo '[uav4] Waiting for MicroXRCEAgent UDP port 8894...' &&
        timeout 90 bash -c 'until ss -lnup | grep -q \":8894 \"; do sleep 1; done' &&
        sleep 2 &&
        source /opt/ros/${ROS_DISTRO:-humble}/setup.bash &&
        [ -f ${ROS_WS:-/ros2_ws}/install/setup.bash ] && source ${ROS_WS:-/ros2_ws}/install/setup.bash;
        ros2 launch peregrine_bringup single_uav.launch.py start_microxrce_agent:=false ros_domain_id:=4 microxrce_port:=8894 uav_namespace:=/uav4 px4_namespace:=/px4_3 target_system_id:=4 use_sim_time:=true
      "
  # -- UAV 5: Peregrine stack + Zenoh bridge --------------------------------
  uav5:
    <<: *common
    container_name: ${PROJECT_NAME:-ros2-px4-flight}-uav5
    environment:
      - DISPLAY=${DISPLAY:-:0}
      - DRONE_ID=5
      - ROS_LOCALHOST_ONLY=1
      - ROS_WS=${ROS_WS:-/ros2_ws}
      - ZENOH_BRIDGE_CONFIG=${ROS_WS:-/ros2_ws}/docker/config/zenoh/uav_bridge.json5
      - ZENOH_PORT=7451
    depends_on:
      sim:
        condition: service_started
    command: >
      bash -lc "
        echo '[uav5] Waiting for MicroXRCEAgent UDP port 8896...' &&
        timeout 90 bash -c 'until ss -lnup | grep -q \":8896 \"; do sleep 1; done' &&
        sleep 2 &&
        source /opt/ros/${ROS_DISTRO:-humble}/setup.bash &&
        [ -f ${ROS_WS:-/ros2_ws}/install/setup.bash ] && source ${ROS_WS:-/ros2_ws}/install/setup.bash;
        ros2 launch peregrine_bringup single_uav.launch.py start_microxrce_agent:=false ros_domain_id:=5 microxrce_port:=8896 uav_namespace:=/uav5 px4_namespace:=/px4_4 target_system_id:=5 use_sim_time:=true
      "
  # -- UAV 6: Peregrine stack + Zenoh bridge --------------------------------
  uav6:
    <<: *common
    container_name: ${PROJECT_NAME:-ros2-px4-flight}-uav6
    environment:
      - DISPLAY=${DISPLAY:-:0}
      - DRONE_ID=6
      - ROS_LOCALHOST_ONLY=1
      - ROS_WS=${ROS_WS:-/ros2_ws}
      - ZENOH_BRIDGE_CONFIG=${ROS_WS:-/ros2_ws}/docker/config/zenoh/uav_bridge.json5
      - ZENOH_PORT=7452
    depends_on:
      sim:
        condition: service_started
    command: >
      bash -lc "
        echo '[uav6] Waiting for MicroXRCEAgent UDP port 8898...' &&
        timeout 90 bash -c 'until ss -lnup | grep -q \":8898 \"; do sleep 1; done' &&
        sleep 2 &&
        source /opt/ros/${ROS_DISTRO:-humble}/setup.bash &&
        [ -f ${ROS_WS:-/ros2_ws}/install/setup.bash ] && source ${ROS_WS:-/ros2_ws}/install/setup.bash;
        ros2 launch peregrine_bringup single_uav.launch.py start_microxrce_agent:=false ros_domain_id:=6 microxrce_port:=8898 uav_namespace:=/uav6 px4_namespace:=/px4_5 target_system_id:=6 use_sim_time:=true
      "
  # -- UAV 7: Peregrine stack + Zenoh bridge --------------------------------
  uav7:
    <<: *common
    container_name: ${PROJECT_NAME:-ros2-px4-flight}-uav7
    environment:
      - DISPLAY=${DISPLAY:-:0}
      - DRONE_ID=7
      - ROS_LOCALHOST_ONLY=1
      - ROS_WS=${ROS_WS:-/ros2_ws}
      - ZENOH_BRIDGE_CONFIG=${ROS_WS:-/ros2_ws}/docker/config/zenoh/uav_bridge.json5
      - ZENOH_PORT=7453
    depends_on:
      sim:
        condition: service_started
    command: >
      bash -lc "
        echo '[uav7] Waiting for MicroXRCEAgent UDP port 8900...' &&
        timeout 90 bash -c 'until ss -lnup | grep -q \":8900 \"; do sleep 1; done' &&
        sleep 2 &&
        source /opt/ros/${ROS_DISTRO:-humble}/setup.bash &&
        [ -f ${ROS_WS:-/ros2_ws}/install/setup.bash ] && source ${ROS_WS:-/ros2_ws}/install/setup.bash;
        ros2 launch peregrine_bringup single_uav.launch.py start_microxrce_agent:=false ros_domain_id:=7 microxrce_port:=8900 uav_namespace:=/uav7 px4_namespace:=/px4_6 target_system_id:=7 use_sim_time:=true
      "
  # -- UAV 8: Peregrine stack + Zenoh bridge --------------------------------
  uav8:
    <<: *common
    container_name: ${PROJECT_NAME:-ros2-px4-flight}-uav8
    environment:
      - DISPLAY=${DISPLAY:-:0}
      - DRONE_ID=8
      - ROS_LOCALHOST_ONLY=1
      - ROS_WS=${ROS_WS:-/ros2_ws}
      - ZENOH_BRIDGE_CONFIG=${ROS_WS:-/ros2_ws}/docker/config/zenoh/uav_bridge.json5
      - ZENOH_PORT=7454
    depends_on:
      sim:
        condition: service_started
    command: >
      bash -lc "
        echo '[uav8] Waiting for MicroXRCEAgent UDP port 8902...' &&
        timeout 90 bash -c 'until ss -lnup | grep -q \":8902 \"; do sleep 1; done' &&
        sleep 2 &&
        source /opt/ros/${ROS_DISTRO:-humble}/setup.bash &&
        [ -f ${ROS_WS:-/ros2_ws}/install/setup.bash ] && source ${ROS_WS:-/ros2_ws}/install/setup.bash;
        ros2 launch peregrine_bringup single_uav.launch.py start_microxrce_agent:=false ros_domain_id:=8 microxrce_port:=8902 uav_namespace:=/uav8 px4_namespace:=/px4_7 target_system_id:=8 use_sim_time:=true
      "
  # -- UAV 9: Peregrine stack + Zenoh bridge --------------------------------
  uav9:
    <<: *common
    container_name: ${PROJECT_NAME:-ros2-px4-flight}-uav9
    environment:
      - DISPLAY=${DISPLAY:-:0}
      - DRONE_ID=9
      - ROS_LOCALHOST_ONLY=1
      - ROS_WS=${ROS_WS:-/ros2_ws}
      - ZENOH_BRIDGE_CONFIG=${ROS_WS:-/ros2_ws}/docker/config/zenoh/uav_bridge.json5
      - ZENOH_PORT=7455
    depends_on:
      sim:
        condition: service_started
    command: >
      bash -lc "
        echo '[uav9] Waiting for MicroXRCEAgent UDP port 8904...' &&
        timeout 90 bash -c 'until ss -lnup | grep -q \":8904 \"; do sleep 1; done' &&
        sleep 2 &&
        source /opt/ros/${ROS_DISTRO:-humble}/setup.bash &&
        [ -f ${ROS_WS:-/ros2_ws}/install/setup.bash ] && source ${ROS_WS:-/ros2_ws}/install/setup.bash;
        ros2 launch peregrine_bringup single_uav.launch.py start_microxrce_agent:=false ros_domain_id:=9 microxrce_port:=8904 uav_namespace:=/uav9 px4_namespace:=/px4_8 target_system_id:=9 use_sim_time:=true
      "
